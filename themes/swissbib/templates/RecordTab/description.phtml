<?
	// Set page title.
$this->headTitle($this->translate('Description') . ': ' . $this->driver->getBreadcrumb());

// Grab clean ISBN for convenience:
$isbn = $this->driver->getCleanISBN();

// Activate Syndetics Plus if necessary:
if ($this->syndeticsPlus()->isActive()) {
    $this->headScript()->appendFile($this->syndeticsPlus()->getScript());
}

?>


<!-- Tab-Inhalte gemaess TP detailstab -->

<? $stdID = $this->driver->getStandardNumbers(); if (!empty($stdID)): ?>
    <?$contentDisplayed = true; ?>
    <h4>Standard-Nummern (ISBN, ISSN, ISMN, DOI, URN)</h4>
        <? if ($stdID): ?>
    <p><?=$this->escapeHtml($stdID[0])?></p>
    <? endif; ?>
<? endif; ?>

<? $mainAuthor = $this->driver->getPrimaryAuthor(); if (!empty($mainAuthor)): ?>
    <?$contentDisplayed = true; ?>
    <h4>Hauptautor</h4>
    <p><?=$this->escapeHtml($mainAuthor[name]) . ', ' . ($mainAuthor[forname])?></p>
<? endif; ?>

    <h4>Alternativtitel</h4>
    <p>hier 240 etc.</p>

    <h4>Reihe</h4>
    <p>490 etc.</p>

    <h4>diverse 500</h4>
    <p>diverse 500</p>

    <? $topics = $this->driver->getTopicalTerms(); if (!empty($topics)): ?>
        <?$contentDisplayed = true; ?>
        <h4>Themen (in Arbeit)</h4>
        <p><?=$this->escapeHtml($topics[0][term])?></p>
    <? endif; ?>