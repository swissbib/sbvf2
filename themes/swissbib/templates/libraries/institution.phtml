<div class="institution">
    <h4>
        <?= $institution['label'][$this->layout()->userLang] . '  (' . $institution['bib_code'] . ')' ?>
    </h4>
    <div>
        <? if(!empty($institution['address'])): ?>
            <p><?= $institution['address'] ?></p>
        <? endif; ?>
        <? if(!empty($institution['city']) && !empty($institution['zip'])): ?>
            <p><?= $institution['zip'] ?> <?= $institution['city'] ?></p>
        <? endif; ?>
        <? if(!empty($institution['url']) && is_array($institution['url'])): ?>
            <? $url = $institution['url'][$this->layout()->userLang]; ?>
            <p><a href="<?= $url ?>" class="color" target="_blank"><?= $this->transEsc('Library_Information') ?></a></p>
        <? endif; ?>
        <p><a href='/Search/Results?type=AllFields&filter[]=institution%3A"<?= $institution['bib_code'] ?>"'
              class='color'><?= $this->transEsc('hold_backlink') ?></a></p>
    </div>
</div>