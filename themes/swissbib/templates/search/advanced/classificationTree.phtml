<div id="jstree-<?= $facetKey ?>" class="jstree jstree-default classification-tree">
    <? foreach($classificationTree as $classification){
        echo $this->render(
            'search/advanced/classificationTreeItem',
            array(
                'classification' => $classification,
                'facetKey' => $facetKey
            )
        );
    }
    ?>
</div>