<? foreach ($tabHeadConfigs as $tabKey => $tabHeadConfig): ?>
<!-- begin: tabbed <?=$tabKey?> -->
<div class="tabbed tabbed_selected tabbed_<?=$tabKey?>">
    <? if($tabHeadConfig['selected']): ?>
    <? foreach ($tabHeadConfig['templates']['sidebar'] as $templateKey => $sidebarTemplate): ?>
        <!-- begin:<?=$templateKey?> -->
        <?=$this->partial($sidebarTemplate)?>
        <!-- end:<?=$templateKey?> -->
    <? endforeach; ?>
    <? else: // Render placeholder for lazy-loaded inactive tab ?>
       <h3>Lade Filter</h3>
       <div class="filters">
            <div class="ajax_loading_spinner_transp" style="width:32px; height:32px;"></div>
       </div>

    <? endif; ?>
<!-- end: tabbed <?=$tabKey?> -->
</div>
<? endforeach; ?>