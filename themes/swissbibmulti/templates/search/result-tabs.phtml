<? foreach ($tabHeadConfigs as $tabKey => $tabConfig): ?>
    <? if($tabConfig['selected']): ?>
        <?= $this->render($tabConfig['templates']['tab']) ?>
    <? else: // Render placeholder for lazy-loaded inactive tab ?>
        <div class="tabbed tabbed_hidden tabbed_<?=$tabKey?>">
            <h1>Lade <?=$this->escapeHtml($tabConfig['label'])?><h1>
        </div>
    <? endif; ?>
<? endforeach; ?>
